# rustfmt configuration — stable channel only.
# Run: cargo fmt --manifest-path src-tauri/Cargo.toml

edition      = "2021"
max_width    = 100
tab_spaces   = 4
hard_tabs    = false
newline_style = "Unix"

# Stable import/module ordering
reorder_imports = true
reorder_modules = true
